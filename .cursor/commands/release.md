Cut a Paseka release.

Read and follow `.cursor/skills/release/SKILL.md` end to end.

Optional bump argument: `$ARGUMENTS` — one of `patch` (default), `minor`, `major`, or an exact version like `v0.2.0`.

Do not tag or push until the user confirms the proposed version and release notes.
